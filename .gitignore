# --- Python ---
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Node / UI ---
ui/node_modules/
ui/dist/
node_modules/

# --- Built web assets (regenerated by `npm run build`) ---
cairn/static/

# --- Local data & secrets (the live DB lives in ~/.cairn, outside the repo) ---
*.db
*.db-wal
*.db-shm
*.sqlite*
.env
.env.*
config.json

# --- Tests (excluded from the public repo) ---
/tests/

# --- OS / editor ---
.DS_Store
*.log
